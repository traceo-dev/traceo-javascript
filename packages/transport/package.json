{
  "name": "@klepper/transport",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": "false",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "run-s lint:prettier",
    "lint:prettier": "prettier */**/*.{js,ts,tsx} --write",
    "pack": "npm pack",
    "clean": "rimraf dist",
    "test": "jest",
    "test:watch": "jest --watch --notify"
  },
  "dependencies": {
    "tslint": "^6.1.3",
    "typescript": "^4.6.2"
  }
}
